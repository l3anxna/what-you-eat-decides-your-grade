[project]
name = "what-you-eat-decides-your-grade"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.1",
    "pandas>=3.0.3",
    "torch>=2.13.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
eda = [
    "matplotlib>=3.11.0",
    "seaborn>=0.13.2",
]

[tool.pytest.ini_options]
addopts = "--ignore=tests/docker"
